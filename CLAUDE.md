# CLAUDE.md — 项目开发指南

> 本文件是给 AI 助手的最高优先级指令。

## 核心规则

### 1. SPEC.md 是项目的大脑

**每次写代码之前，先读 SPEC.md。**

- `SPEC.md` 是项目唯一的权威规格文档，优先级高于所有其他文件
- 任何非平凡的代码改动，第一步永远是 `Read SPEC.md`
- 如果 SPEC 中没有描述某个功能，先和用户确认是否应该加入 SPEC
- 代码实现必须与 SPEC 中的架构设计保持一致

### 2. 按优先级逐步推进

SPEC 第三节使用 P0-P3 优先级体系：

| 优先级 | 含义 | 策略 |
|--------|------|------|
| 🔴 P0 | 数据质量根基 | 阻塞性，必须最先完成 |
| 🟠 P1 | 核心功能 | P0 完成后推进 |
| 🟡 P2 | 体验增强 | P1 稳定后做 |
| 🟢 P3 | 长期愿景 | 资源允许时考虑 |

**原则**：当前优先级未完成前，不跳到更低优先级。用户说"继续"时，默认推进当前未完成的最高优先级任务。

### 3. 架构约束

- **后端**：FastAPI + LangGraph 多Agent工作流（v3.0）
- **前端**：Streamlit (app.py) + Chrome Extension (extension/)
- **LLM**：DeepSeek v4-pro，不要改用其他模型
- **向量库**：ChromaDB + bge-small-zh (512维)
- **多Agent模式**：Planner → Router → 3路并行检索(asycio.gather) → Fusion → Writer → 3路并行评审 → 多数表决

### 4. 代码风格

- 中文注释和文档
- 类型标注（TypedDict / Pydantic）
- 异步优先（async/await）
- 错误隔离（单个Agent失败不影响整体）
- 退路机制（LLM不可用时规则化降级）

### 5. LLM 使用边界

| ❌ LLM 禁止 | ✅ LLM 允许 |
|------------|------------|
| 概括/改写简历内容 | 字段标准化映射（"Vue3"→"Vue.js 3.x"） |
| 编造/推断缺失信息 | 智能位置匹配（简历内容→表单字段的对应关系） |
| 生成简历中没有的内容 | 分类判断（内容是"技能"/"项目"/"成果"） |
| 压缩/摘要原始文本 | 技术栈归类（"LangGraph"→类别"ai"） |

### 6. 禁止事项

- 不要在没有更新 SPEC.md 的情况下做大的架构改动
- 不要绕过 SPEC 中的优先级体系
- 不要删除或削弱现有的退路/降级逻辑
- 不要在简历解析层使用 LLM 概括/改写内容
